<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Upside Arena agent notes

- **No em dashes, and nothing else that reads as generated, in anything a person sees. The whole of `src/`, not a list of screens.** Product copy has to sound like a person wrote it, because in Arena every sentence is one: there is no model-written copy in this app at all, so nothing was scrubbing them and nothing was going to. A dash used as a clause break is the loudest single tell that a sentence was generated, so reader-facing strings take a comma, a colon or a full stop instead, and number ranges are "2 to 3 weeks" or "2028-2029". `tests/unit/reader-copy.test.ts` walks **every** `.ts`/`.tsx` under `src/` and fails on an em or en dash in any reader-facing line. It skips block comments, `//` comments (string-aware, so a URL is not mistaken for one) and `*.test.ts*`, because source is exempt: the rule is about the product. `ALLOWED` is an **exception** list, currently one file (`src/lib/brand/mono-subset.ts`, which names the characters in a Unicode block so the subsetter keeps them in the font). A test keeps it honest: an entry naming a file that no longer contains a dash fails, so it cannot become a parking space for unfixed copy. **Never add a file to `ALLOWED` to make a failure go away.** A separate scan for AI-tell vocabulary (delve, seamless, robust, elevate, empower, cutting-edge, in today's fast-paced, and forty more) found **nothing** in hand-written copy: the dashes were the whole problem. `/how` carried twelve of them and is now clean.
- **A cell with no number in it says `NO_VALUE`, and `NO_VALUE` is "n/a".** `src/lib/format.ts` exports it; it is the fallback in every `Score` card on Home, in a battle and in a record, and in `StandingsTable`, `SeasonTable` and `Lineup`. It was an em dash. **Do not "fix" it to a bare hyphen**: these sit beside signed percentages in `tabular-nums` columns where a loss already renders with a leading `-`, so a lone `-` reads as a number whose digits failed to load.
- **What Arena will buy is American, in dollars, and at least a dollar a share. Three rules, one choke point each.** The quote layer (`src/lib/market/quotes.ts`) refuses anything not quoted in `USD` and anything listed on an over-the-counter venue (`OTC_EXCHANGES`), because both are questions of whether a thing can be priced at all: BMW.DE quotes at 58.52 **euros**, and until this existed a search for BMW offered it, Arena spent dollars at that number, and the position was then valued in euros as though the two were the same money. `checkTrade` (`src/lib/game/formats.ts`) refuses a **buy** under `MIN_SHARE_PRICE`, which is $1, wherever a format's universe is a *kind* of thing rather than a named list. That floor is the whole answer to the cheapest way to win a leaderboard here: HCMC was quoted at **$0.0001** the day this was written, a hundred thousand dollars of it is a billion shares, and the tick to $0.0002 is a week up 100% that took no judgement at all. **Selling is never refused** by any of it, so a name that falls through the floor is a bad week and not a trap, and the hand-picked list formats are exempt on purpose: a coin at forty cents is an ordinary coin. The lineup checks it twice, at queue time as a courtesy and at the fill against the **opening** price, which is the one that counts, and a refused order says the rule rather than "we had no price". Search uses an **allowlist** of American venues while the quote layer uses a **blocklist** of OTC ones, and the asymmetry is deliberate: pricing should survive a venue nobody thought of, while nothing findable in the search box may be refused two clicks later. Every venue code in both lists was read off real Yahoo responses, never guessed.
- **A week ends even when one company in it cannot be priced, and the caller has to say which.** Refusing to score a week with an unpriced holding (migration `0015`) is right: valuing it at zero writes a wipeout that never happened and scoring is idempotent, so the wrong number is the final one. On its own it deadlocks, and the trigger is ordinary. A company is acquired, delisted, renamed or halted on the Thursday, one player holds it, and from that Friday every settlement pass raises, releases its claim and runs again forever: **nobody** in that week is scored, and from the outside it is indistinguishable from a week that has not finished. So `score_cycle` takes `p_at_cost` (migration `0026`), and a company named there is worth **what was paid for it**, which is a price Arena really saw and is exactly what the screen has shown that holding as worth all week (`positionValue` with a null price). A symbol that is neither priced nor named **still stops the week**, so the refusal is intact for everything except the case the caller has taken responsibility for. `planForUnpriced` (`src/lib/game/settle.ts`) is what takes it, and it is pure so it can be tested: it waits until `PRICE_GRACE_HOURS` (**6**, so a normal week still settles on the Friday evening and a held-up one has a result before Saturday morning) and refuses to give up at all when half or more of the week's symbols are missing, because delistings arrive one at a time and half a week going at once is the provider. **Do not shorten the grace or drop the outage guard**: below them, a passing Yahoo outage settles somebody at cost while the market has them at twice that.
- **A share split is not a loss, and Arena applies it in the morning.** Nvidia split ten for one on 10 June 2024: a hundred shares became a thousand at a tenth of the price and nothing was traded. Every price the app can read is post-split from that open, so a holdings row still saying a hundred values the position at a tenth of the truth and shows somebody who did nothing a **90% loss**, which the week is then settled on. A reverse split is worse because it looks like winning: one for ten leaves the shares at ten times the price and prints a **900% week** nobody earned. Migration `0027` is the answer: `symbol_splits` is the ledger and its primary key is what makes applying one twice impossible, `split_checks` is the day's claim so one worker asks the provider, and `apply_split` does the arithmetic in one transaction. The fraction that will not divide is **paid out in cash at the post-split price**, the way a broker pays it, so the position is worth the same money either side to within one share, and the cost basis follows the shares so the average cost falls by the ratio. **The check runs after the opening bell, never before it** (`hasOpenedToday` in `src/lib/game/splits.ts`, and it does not even take the day's claim while it is early): before the bell the only price there is is yesterday's pre-split one, and paying a tenth of a share at ten times its worth is a worse bug than the one being fixed. Three things are deliberately left alone: a **closed week**, because its result is written and history is not revised; a portfolio that has **traded that company since the split took effect**, because those shares were bought at the new price and are already counted the new way (`holdings_skipped` records it); and **lineup orders**, because an order for a hundred shares is an order for a hundred shares. **A spinoff is not a split and arrives on the same feed**: GE reports a real 1 for 8 reverse and then `1281:1000` and `1253:1000`, which are the HealthCare and Vernova spinoffs and leave the share count alone, so `MAX_SPLIT_TERM` (50, on the reduced fraction) refuses anything quoted in thousandths. Applied as a split, that one would hand every holder 28% more shares at the current price. Upside Lab's `corporate-actions.ts` drew that line first and is worth reading before touching this: **it is deliberately a different design**, because Lab's holdings are typed in by a person, so it reports what a position would be and lets the reader apply it, while Arena's are written by the server and can be adjusted outright. It rides the trading day pass as the first job, before the mark, with `getCurrentCycle`'s `after()` as the backstop. **Dividends are deliberately not handled**: Arena measures price return and the benchmark is measured the same way, so nobody is advantaged, and `/how` says a split is handled rather than leaving somebody to wonder.
- **A season table is ranked in the database and read a page at a time.** `getSeasonView` used to ask for every `season_results` row in the quarter, then every profile behind them, sort the lot in JavaScript and keep the first fifty. That is the shape that stops working first: a season is three months of an open game, and the page it is on is the one people open to see whether they are climbing. `season_standings` (migration `0028`) orders it once over an index, returns the page **plus the caller's own row wherever it is** (somebody 340th needs to see where they stand far more than the top fifty need to see each other), and orders it in **exactly** the way `close_season` awards the final places. That agreement matters more than the speed: a table that reads one way all quarter and hands out its medals in another order on the last day is the app calling itself a liar. Both now break a tie on the **player id**, arbitrary and fixed, the same rule `src/lib/game/ranking.ts` gives the weekly tables; before that, two players level on the average and on weeks played were placed in whatever order Postgres returned, so which of them was champion could change between two page loads. The output column is `place`, because `position` is a reserved word in Postgres.
- **Something asks Arena whether it is working, every half hour, and it is not asking whether the server answers.** `/api/health` (public, uncached, 503 rather than 200 with a sad word in it) asks the three questions that are the app: can the game be read and written, are there prices, and **has a finished week been scored**. The third is why it exists. Settlement is the failure that is invisible from outside, since every page renders and nothing errors while a week quietly never ends, which is exactly what it did until `0026`. `SETTLEMENT_ALARM_HOURS` is **12**, deliberately longer than the six `planForUnpriced` waits, so the alarm cannot fire on a week doing what it was built to do. The answer is three booleans and a few words, no counts, no names, no versions and no configuration, which is what makes it safe to leave open (it is on `PUBLIC_API_PATHS`, since a schedule has no session). `.github/workflows/health.yml` pings it twice thirty seconds apart before calling anything an outage, and a failed scheduled workflow is how GitHub mails the owner, so the whole alerting system costs nothing. `tests/unit/health.test.ts` reads both files and fails if the endpoint stops answering 503, starts being cached, or if the alarm window drops inside settlement's own patience.
- **A failure that tells nobody is a failure found by a stranger writing in.** Until `0029` a screen that would not draw wrote to the browser console, where one person could read it with the console open, and the server's own errors went to the host log, kept for an hour on the plan this runs on. `error_reports` is the answer and it is deliberately a **table**, not a monitoring subscription: keyed on a **fingerprint** of what broke and where, so a page failing for three hundred people is one row with a count of three hundred rather than three hundred rows (a log that floods is a log nobody reads and a database filling with one bug). **It holds nothing about who**: no user id, no address, no session, no headers, and the route is stored with everything after the question mark cut off, because an invite code and a share token both live there. `/api/error` is **not** on `PUBLIC_API_PATHS`, so the proxy answers 401 before the route runs and a signed-out failure goes unrecorded, which is a real gap and a small one: the signed-out surface is one page and `signed-out.spec.ts` walks it every pull request. The session is a **gate, not a field**, and `errors.test.ts` fails if `user.id` ever appears in that route. `reportServerError` is what a catch block calls, and the three background jobs in `getCurrentCycle`'s `after()` all call it, because that work is what nobody is watching. `global-error.tsx` posts by hand rather than importing the helper, since it replaces the root layout and must keep to what cannot fail. Read at `/metrics` under "What broke", ordered by when a thing last happened rather than by how often, because a bug that stopped a week ago matters less than one happening now. `PRIVACY_VERSION` moved with it: a policy that lists what is collected has to list this.
- **The market shuts on ten days a year that are not weekends, and now the app knows which.** `isMarketHoliday` (`src/lib/market/session.ts`) computes the exchange's calendar rather than listing it: the four fixed dates with the exchange's own weekend rules (Saturday moves to the Friday before, Sunday to the Monday after, **except New Year's Day on a Saturday**, which the exchange does not observe), the five weekday rules, and **Good Friday** from the anonymous Gregorian Easter algorithm. No network call, no data file, nothing to maintain. Three real bugs closed with it. (1) A streak: `tradingDaysBetween` counted Thanksgiving as a day missed, so somebody who looked in on the Wednesday and came back on the Friday spent a freeze, or lost the streak, for not opening a game whose market was shut. The old note claiming the error only ever ran in the player's favour had it backwards. (2) **Trading on a holiday** filled at the last close, which on a holiday Monday is **Friday's**: three days of news known and the price not moved yet, which is a timing exploit rather than a rounding error, so `isTradingOpen` now says no. (3) **The lineup**: `lineupReady` said the fill was ready at 10:00 on the Monday whatever day that was, so a week starting on Memorial Day, Labor Day or MLK Day ran every queued order against a session that never printed and returned "we had no opening price for that morning". Three weeks a year of lineups thrown away silently, which is the exact promise the feature was built on. `nextSessionOnOrAfter` is what it asks now. Half days (the afternoons after Thanksgiving and before Christmas) are deliberately **not** modelled: everything Arena does with a session happens before 13:00 or after 16:00. `tests/unit/holidays.test.ts` checks every date against the published calendar rather than against the code.
- **The unsubscribe link in an email turns the emails off, rather than pointing at a sign-in page.** It used to be `${siteUrl()}/profile` for everybody, which asks somebody who has stopped using Arena to sign back into the account they left and find a switch. What they press instead is the button that says spam, and one of those costs the sending domain more than a hundred quiet unsubscribes. `unsubscribeUrlFor` (`src/lib/notify/unsubscribe.ts`) signs an HMAC of the user id, `List-Unsubscribe` carries it and `List-Unsubscribe-Post: List-Unsubscribe=One-Click` tells the client it may post to it without asking, which is what Gmail and Yahoo have required of bulk senders since 2024. **The one-click header only ever goes out beside a link we signed**: on the profile fallback it would be a button that appears to work and does not. `/api/unsubscribe` is on `PUBLIC_API_PATHS`, and what makes that safe is that the signature permits exactly one thing, turning that person's email off. **GET changes nothing and POST does the work**: scanners, previewers and corporate gateways fetch every URL in a message before anybody reads it, so an unsubscribe that fires on a fetch unsubscribes people who never asked. There is **no expiry**, deliberately: a two year old email is the one somebody is most likely to unsubscribe from, and "this link has expired" is a failure at the only job it has. `UNSUBSCRIBE_SECRET` is optional and falls back to the service role key; with neither there is no link and no header, because a forgeable one would let a stranger turn off a stranger's mail.
- **Reduced motion means the page is drawn, not that it is drawn quickly, and the numbers are in `docs/PERFORMANCE.md`.** The blanket `prefers-reduced-motion` rule cut animation **duration** to 0.01ms and left the **delay** and the **fill** alone. `.rise` is `both`, so it holds its first keyframe, which is `opacity: 0`, through a stagger that runs to **0.26s**: somebody who asked for less motion watched a blank hero for a quarter of a second and then had the whole thing appear at once, which is exactly what that setting exists to prevent. Both are zeroed now. The duration deliberately stays at 0.01ms rather than `none`, which is the shape of that rule everywhere it is recommended and keeps an `animationend` listener working. **The landing page reports no Largest Contentful Paint at all**, and it is worth knowing why rather than reading the blank as a good score: Chrome refuses a transparent element as a candidate and never goes back for it, so the hero's fade makes the page unmeasurable in the field. That is the documented trade for a page that arrives rather than appears; the lever, if it is ever wanted back, is the opacity term in `@keyframes rise`. **CLS is 0.000 on every signed-out page** and that is the number to defend. The one figure that is not good enough is the landing's **~330ms long task** at hydration, against 60ms everywhere else. `scripts/vitals.mjs` measures all of it in one command, twice per page, once as most people get it and once as somebody who asked for reduced motion does. **`tests/e2e/keyboard.spec.ts` is the other half of `accessibility.spec.ts`**: a rules engine cannot decide whether the skip link is the first Tab stop, whether a focus ring is actually drawn, whether every stop is something a person can see, or whether the cookie question can be answered and dismissed without a pointer. Those are four questions with no static answer and they are asked in a browser.

- **The signed-out page is a product page, and it is the same page Lab's is.** `SignedOutLanding` (`src/components/`) is composed by `src/app/page.tsx` and follows Upside Lab's flow deliberately, because the two apps are one design and this is the first screen anybody sees of it: problem, the interface directly under it, what it does, how you start, the rest of the rooms, what it costs beside what is at stake, then the ask again, then a footer. What it used to be was a sign-in box with the game in two lines beside it, which meant a stranger judged Arena on two of the eight things it does with no answer to what it costs or what is at stake. Every claim on it lives in `src/lib/product.ts` so the page, the metadata and `/how` cannot end up describing three slightly different games, and nothing aspirational goes in that file. The page stays a **server component** and keeps its Suspense boundaries around the sign-in button alone, so `/` is still Partial Prerender; `Arrive` is the one client piece and it is a wrapper.
- **Three rules hold this page that hold nothing else in the app, and they are all restraint.** (1) **One headline, one line under it, then the thing.** No eyebrow above every section in small coloured capitals: that pattern is everywhere in software marketing and nowhere in Arena, so a page wearing it reads as a page *about* Arena rather than as Arena. Stills carry the app's own small muted label, not invented chrome, because they are there to be believed. (2) **A card gets one line.** The first draft ran three sentences under every heading and two more inside every card, and that much explaining does not read as thorough, it reads as doubt; anything needing a paragraph belongs on `/how`, which is linked and needs no account. (3) **Size is the argument**: 64px hero, 44px section headings, 16px body rather than 14px. The copy is short enough to afford it, and that is the trade. **Cut the sentence, not the type**: the hero's two lines are 26 and 19 characters because that is what holds one line each at 64px, and a 44-character draft forced 44px and *still* wrapped, at which point `text-wrap: balance` split it mid-phrase.
- **One width, and every pane on the page shares its edges.** `max-w-5xl`, hero card included. The stills briefly sat in a 1200px container on the theory that a picture of the app should be as wide as the app, which put three different left edges down one page (336, 120, 208 at 1440px). That does not read as emphasis, it reads as a page assembled from parts: the eye tracks a vertical edge far more readily than it infers a container's intent. The only narrower measure is the run of centred text in the hero, which is a measure rather than an edge. Section heads are centred for the same reason, since the hero and the closing ask both are.
- **Google is the only way in, and that is why the closing ask is a real button.** The magic link went on 2026-08-23 (`signInWithEmail`, `email-mx.ts` and the address-suggestion flow with it). Everything it did served getting an address right that Google already has right, and every step of it was a way for somebody to fail to reach their own account. Because signing in is now one button, the ask at the bottom is the ask rather than a link back up to one, which is what Lab's does; while the email field existed it had to be an anchor, since a second copy meant a second form, a second control labelled "Email" and one visitor counted twice. **`signin_viewed` is on the page, not in the card** (`TrackView` in `page.tsx`), because the card renders twice. **The consent sentence renders beside *every* sign-in button**, not once at the top: consent belongs where the action is. The advice disclaimer and the support address moved out of the hero into the footer, because four lines of grey small print under the only control on the screen is the loudest possible way to say there is small print. `terms_acceptances`, written at onboarding, is still the durable record of who agreed to what, so nothing about that record depended on the magic link. The signed-out suite needs `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` placeholders the way it already needed Supabase ones, set in `playwright.config.ts` and in the CI job; without them the button does not render and half the landing tests fail for a reason unrelated to the change under review.
- **`Arrive` never hides anything already on screen.** `src/components/Arrive.tsx` measures the rect first and marks a section arrived without passing through the hidden state when it is at or above the fold. Without that check, hydrating hides what the reader is looking at and fades it back a frame later, and anybody who arrived part way down the page, from a link with a hash, a reload, or restored scroll position, watches the page they are reading disappear. The attribute is written straight onto the node rather than through React state, and a browser with no `IntersectionObserver` gets no attribute at all and a page that is simply visible. **Do not put `scroll-behavior: smooth` back on the landing.** It was there for the `#start` anchor, the anchor is gone with the email field, and it made every programmatic scroll-into-view animate, which is how a click probe in `dock.spec.ts` started landing before the scroll settled.
- **Nothing on the landing arrives where a reader can watch it.** `ARRIVE_LEAD` (`src/components/Arrive.tsx`) is **1.25**, a screen and a quarter, and it reaches the observer as a **positive** bottom `rootMargin`. It shipped as `-12%`, which is the opposite: a negative margin shrinks the observer's root rather than growing it, so measured on the real page every section flipped to "in" while sitting **116px to 185px inside the window** and only then started its fade. What a reader saw where the next section should be was an empty band and then something slowly appearing, which reads as the page having ended, so back up they went. The mount check is the same rule, not a second one: a section **inside the lead** at load is marked arrived without ever being hidden, so nothing flashes on the first screen, a reload part way down draws its surroundings, and the screenful after the one you are looking at is finished before you reach it. It used to stop at the fold, which left the section immediately below it fading in because somebody scrolled. **A heading and the row it heads go in one `Arrive`**: they were two, the cards on an 80ms delay, which is how a title came to sit above a hole, and there is no per-section delay any more. `Section` is `py-10 sm:py-12`, so 80px between sections on a phone and 96px on a desktop. It was `py-14 sm:py-20`, and the ratio that defends is the one against the section; the ratio a scrolling reader lives is against the **window**, where 160px is 18% of a 900px screen and was the tallest empty band on the page. The fade stays 0.55s and 12px, deliberately: with the lead above it nobody watches it, and the number was chosen with a reason beside it. Upside Lab's `Reveal` is this component with the same lead and the same guard, and it had the same bug for the same reason, so fix both or neither. `tests/unit/landing-scroll.test.ts` reads the source, and `tests/e2e/signed-out.spec.ts` walks the page in 100px steps and fails if any section is still unpainted while on screen.
- **The cue at the fold is in the page, not over it, and it is drawn only when the page is not already saying it continues.** `ScrollCue` (`src/components/`) used to be a `glass-notice` pill fixed to the bottom of the window whenever the document was taller than the screen, and both halves of that were wrong. **It was the reason the landing filled in as you scrolled it on a phone.** A `backdrop-filter` pinned over moving content re-filters its backdrop every frame, and the bottom band of the window is exactly where new content arrives while you scroll: measured at 412x915 with the CPU throttled ten times, one pass down the signed-out page presented **42 frames the compositor had to repaint**, the worst of them with **38% of the bottom eighth of the screen** not yet caught up with where the page actually was. Hiding that one element and changing nothing else took the same scroll to **9 frames, every one pixel-identical to the settled page**. What a reader saw was the lower half of the sample card arriving late. So the rule is the pair rather than either half: **nothing on this page may be `position: fixed` over the content and carry a `backdrop-filter`.** The bottom fade went with the pill, because a scrim that fades out a severed card is fading out the strongest scroll cue the page has. What replaced it is a line of the page's own type, no fill and no rim, laid out inside the hero at `top: calc(100svh-3.5rem)` so it sits in the last band of the **first** screen and then scrolls away with everything else; it reads its own rect back, so a wrong position draws nothing rather than something in the wrong place. **Three cases, measured on every resize, and only one of them wants words**: a sample card cut by the fold says it loudest and gets nothing (136px past the edge at 412x915, 237px at 390x844, so every phone), the next section's content having reached the screen says it too and gets nothing (the hero's 9rem floor puts 96px of a heading on a tall display, which is why that number is 9rem and not less), and a first screen that ends clean, roughly a 960 to 1016 CSS pixel window, is the one case the cue exists for. There is a narrow band either side where the card ends within the cue's own height of the fold and neither is available; saying nothing beats crowding the card. It re-reads on `load` and on `document.fonts.ready` as well as on resize, because it decides on a few pixels of clearance and a font swap moves more than that: measured on Lab the card sat 11px clear of the band, and one measurement taken at hydration got it wrong. `tests/unit/scroll-cue.test.ts` fails if the cue goes back to being fixed or grows any glass, and `tests/e2e/signed-out.spec.ts` works out which of the three cases the window it is running in produced and holds the cue to it. **Upside Lab's copy is the same component under the same rules and the same guards, so fix both or neither.**
- **The landing's light travels with the page. `.landing-field`, never `.page-frame` alone.** `.page-frame::before` is `position: fixed`, which is right in a room (the lobes are the room's lamps and a room is one viewport of content) and wrong on a page five screens tall, where the cyan corner you arrive on is the same cyan corner five screens later: what reads is not a lit room but a colour that stopped. `.landing-field` (`globals.css`) kills the fixed pair with `display: none` and puts five lobes on `::after`, sized in `vh` and positioned in `%` of the whole document, alternating `--primary` and `--glow-secondary` so scrolling passes through them. The magenta stays much quieter than the cyan, as it is everywhere else. Measured against `/how`: the fixed field reads 7,87,95 at the top of the page and 7,87,95 at the bottom; this one runs 6,68,75 / 26,15,26 / 4,48,53 / 25,14,25 / 4,51,56, and **the middle of the frame never rises above 4 out of 255**. That last number is the one that matters. Do not brighten it, and judge any change on the page middle rather than on how much of the field is lit.
- **The walkthrough's rows are `.glass`, not `CARD`.** `CARD` (`src/lib/page-shell.ts`) is `glass-well`, the quiet nested material keyed to `--muted`, and it is right for a well sitting inside a panel that is already doing the refraction. The walkthrough has no panel around it: its rows sit straight on a dialog over an 80% scrim, where `glass-well` reads as a flat grey box. `ROW_GLASS` in `src/components/WelcomeTour.tsx` uses the top-level material instead (`card-sheen glass`), which is what carries the three specular terms that sell glass on a near-black field. Anything else drawn directly on an overlay wants the same treatment.
- **One walkthrough, versioned, and everybody sees it once.** `WelcomeTour` (`src/components/`) opens over Home with Home already painted behind it. Whether it shows is `profiles.tour_version` against `TOUR_VERSION` (`src/lib/tour.ts`); raising the constant re-shows it to every account exactly once, and that is the whole reset mechanism. `needsTour` returns **false** for `undefined`/`null`, so a deploy that lands before its migration shows nobody a walkthrough rather than showing everybody one on every page load. Escape, the X and "Skip the tour" all write the version down: a walkthrough that returns tomorrow because you dismissed it today is a nag with a progress bar. Nothing in `src/lib/tour-steps.ts` is a figure typed by hand, and `tests/unit/tour.test.ts` holds it to that. Every screen is in `/gallery` so `tests/e2e/clipping.spec.ts` measures it at every width a phone reports.
- **The dock is chrome, not a card, and it says the name of every room you touch.** `BottomDock` is `card-sheen glass glass-dock`; `glass-dock` (`globals.css`) replaces the body and the blur only, `--background` at 55% and a harder blur in place of the 2% white veil and 6px, leaving the rim, the ring and the lift shadow untouched. A card is built to let the room through; a dock sits over the bottom corner with content scrolling under it, and at the card's numbers it measures nearly **twice the colour of the field beside it**. Two constraints fix the 55% and it is the smallest number meeting both: the pane's own chroma must not exceed the field's, and muted label text must clear AAA over every pixel of the bar. Do not lower it to let more glow through, and do not raise it either.
- **The shape is one capsule that hugs its contents, and on a phone it carries no words.** Four glyphs and the player's own face, `rounded-full` shell, `p-1`, `rounded-full` cells, which is the one radius pair that stays concentric at any size. At `md` and up the same capsule paints the label beside each glyph. That asymmetry is the input device, not an inconsistency: a pointer has no press to speak on, a desktop dock floats in a metre of empty page so the words cost nothing, and Lab's desktop dock carries one cell per portfolio, whose names are somebody's own words and can never be a glyph. **The material, the radius and the marker are identical across the breakpoint; only the word count differs.** Upside Lab's `MobileTabBar` and `BookModeDock` are the same pair, and the two apps are meant to stay one design.
- **"Never hide a dock label at a breakpoint" is retired, and what replaced it is a stronger promise: a room names itself the first time you touch it.** The old rule went in after `max-[544px]:sr-only` hid all five labels on every phone anybody owns, leaving a row of glyphs and nothing to read, and it was right about the failure and wrong about the fix: it was a ban on a symptom. What was ever being defended is a person's ability to find a room they have not been to, and a painted 11px word under a 16px glyph is a weak way to defend it, since it is there for the thousandth visit as much as the first and plenty of people never read it. So on **`pointerdown`**, before the tap has finished and long before the room answers, the pressed cell's name rises above the dock in the same glass and is gone inside `SAY_MS` (900ms). At rest there is not a word on screen; in use there is never a tap that does not name itself. **`pointerdown`, never `click`**: a name that arrives after the tap it was meant to answer is a name nobody needed. `onFocus` fires it too, because a keyboard never presses anything. The chip is `md:hidden`, since above the breakpoint the label is already painted and a chip would be the same word twice. This also replaced the old `PressedFill`, which said "heard you" and nothing else: the chip says "heard you, and it was Leagues", which is the same reassurance plus the one thing a new player is missing. `tests/e2e/dock.spec.ts` asserts the accessible name, both handlers and the breakpoint, and still measures every painted label against **the padding box of its own cell**, because a row that fits the screen tells you nothing about a word inside a 52px cell.
- **One marker, and it travels.** A single neutral pill (`bg-foreground/10`) sits behind the cells and slides, rather than a fill appearing on one cell and disappearing from another. That is what makes a row of glyphs read as one place with a marker in it instead of four buttons. It is **measured off the live cell** on every pathname change and through a `ResizeObserver` on the row and each cell, because the face cell and the glyph cells are only the same width by agreement and the labelled cells at `md` are not; it is held still until it has been placed once, or the first paint draws a marker sliding across a bar nobody has touched. **The accent is not spent in the dock at all.** Which room you are in is the least surprising fact on the screen, and the old `bg-primary` cell was the loudest thing on the bar for the least reason.
- **Season is not in the dock, and the face is not in the header.** A bar is quiet because it holds few things, not because the things in it are drawn small. Season is the one room that cannot change between Monday and Friday, since every figure in it was settled on a Friday and has not been touched since: that is a record rather than a room, it lives with the rest of a player's record on Profile, and `/profile` linked to it long before it left the bar. Taking it out also takes the calendar glyph away from the trophy, which was the one pair somebody could genuinely mix up on a bar with no words on it. The **face** moved the other way, out of `AppHeader` and into the dock's last cell, because two pictures of the same player on one screen is one too many and the one to keep is the one under the thumb; `(app)/layout` streams it in as the dock's `me` prop, so the rest of the bar is still prerendered shell that waits on no session. Raising `TOUR_VERSION` was part of this, not an afterthought: version 1 told every player there were five rooms and pointed at a picture in the top corner that is no longer there.

- **The ambient field is dithered, because eight bits are not enough for it, and the dither's mean must stay exactly zero.** Both lobes are a low-alpha wash on `oklch(0 0 0)`, so the whole ramp is spent in the bottom of the range: about thirty luminance levels of 255 across most of a screen, which is a 1/255 step held flat for twenty-odd pixels. That is a contour line, not a gradient, and Arena's lobes are **two stop ramps, colour straight to transparent**, so they had it worse than Lab's five stop ones. Re-shaping the stops cannot fix it and neither can a blur (re-quantising a blurred staircase returns the same staircase); the levels are not there to be spent. `AmbientDither` (`src/components/AmbientDither.tsx`, rendered once from the root layout) is the answer, carried by `.page-frame::before`, the landing page's own `.landing-field::after` and `.ambient-glow`; the four profile themes override the gradients only, so they inherit it, and none of them may grow its own `filter`. **Signed and clipped, both load-bearing.** Every CSS blend mode that lays grain over a page adds light, measured at +1.6 levels for a `plus-lighter` tile and +2.3 for `overlay`, which is the true black the product is built on turned grey; inside a filter chain the noise is added and half of it subtracted again, and both terms are masked by `SourceAlpha` so a pixel the lobes never reach comes back untouched. Measured here: a true black patch reads 0.00 with a spread of 0.00 after the dither, and a lit patch went from a spread of 1.40 to 2.43 with its mean moving 0.08. **Amplitude is two levels and it was measured**: one left the rings findable, three made the grain itself the artefact. It is **inline, never a data URI** (Safari does not resolve a filter from one, which would leave every iPhone undithered with nothing failing) and one octave at `baseFrequency="0.8"`, so grain rather than clouds. The two amplitude numbers are typed separately and nothing on the page announces it when they drift, so `tests/unit/ambient-dither.test.ts` fails if they stop being exact double and half, if either term loses its clip, if a surface drops the filter, or if a theme adds one. **The glow behind the sample card is `.ambient-glow`, never a hand-rolled ramp** — it was `bg-gradient-to-br from-primary/12 to-transparent opacity-70 blur-2xl`, one colour and one stop across a card's worth of near-black, which had about twenty levels to spend and stepped every thirty-odd pixels on top of the rings the field was already drawing; it is a four-stop falloff now with no blur term. **Lab has the same file, the same numbers and the same guard. The two apps are one design, so fix both or neither.** Judge any change here by measuring black lift and riser-to-noise against `/gallery`, never by eyeballing.
- **Glass is a 2% white veil with all of its light on the rim, and the black inside a pane is the black of the room.** The body is `--glass-veil` (`oklch(1 0 0 / 2%)`), **white and achromatic, never a tinted card**. Two measured reasons. (1) A tinted fill leaves a hue behind: thinned over a lit field a neutral card lands somewhere between its own bias and the room's, which is a colour nobody chose. White has no hue to leave. (2) **The veil's alpha is what sets the black floor** — the old fill lifted a pane about nine luminance levels out of 255 above the field beside it, and a pane whose blacks are grey is a card, not glass; at 2% the lift is near zero. **`--glass-contrast` (1.05) is the second lever and is not optional**: contrast pivots on mid-grey so it leaves true black alone, but this field is not true black (the cyan and magenta lobes sit at about five levels) and without it a pane lifts those six. Measured 1.00 → +6.1, 1.02 → +3.1, 1.05 → +0.1. **Saturation is free**: 1.75 and 2.40 measure identically on black lift. `--glass-blur` is **6px, not 40** — past roughly 7px a blur stops refracting and starts mixing adjacent hues into mud. The rim came down with the fill (`--glass-rim-top` 38%, not 56%) because at this transparency the old weight read as a drawn outline rather than as light; it still takes the room's two hues, cyan on top from `--primary` and magenta underneath from `--glow-secondary`, with the sides unequal because the key light is off the left. **Wells are panes in their own right** (`--glass-veil-well` 3%, own 5px blur, softest rim in the system), not grey boxes on a pane. `.glass-notice`, `.glass-bar` and `.glass-dock` keep heavier fills and a real blur because they sit over content, which is the only place a heavy blur earns its keep. `.glass-dock`'s fill is keyed to `--background` rather than `--card`, so its alpha is exactly how much of the ambient glow the dock eats and it cannot leave a hue behind. Never reintroduce a falloff, a `background-image` ramp, or a tinted card fill on any glass surface. Judge a change by measuring black lift and the material's own chroma against `/gallery`, never by eyeballing.
- **One account, however many mailboxes, and the player tag never moves. Google adds them, and nothing else does.** A person signs up on a laptop with one Google account and then taps Continue with Google on their phone with another, which is a different address and, until `account_emails` (migration `0025`), a different account: a second player tag, a record starting at zero, and a league nobody is in. **No second auth user is ever made.** Supabase still holds exactly one, with one primary email, and the extra addresses are checked before a sign-in is turned into a session: the Google callback asks `accountForAddress` first, and when it answers, the session comes from a one-time token minted for the account that owns the address (`magicTokenFor`, spent by `verifyOtp`) rather than from Supabase's own idea of who that address is. **Adding is `connectGoogle`**, the same handshake as signing in, with `link` written into the state cookie before the browser leaves because nothing coming back from Google can say what the trip was for; the address is written down already verified, since the handshake is the proof. Adding is the service role's work (the check is the whole job, and a client checking itself is not a check); the one write a client may make to that table is deleting a row of its own, and row level security says so. **The one destructive case is narrow and the database decides it.** An address that already has an Arena account can be taken only when `account_never_played` says that account has no tag, no finished onboarding, no trade, no league, no shared week and nothing bought, which is somebody who signed in once, was asked for a player tag and closed the tab. Two accounts that have both been played are refused and sent to support, because choosing which one loses its weeks is not a decision code gets to make. Every outcome is a word in `ADDRESS_MESSAGES` (`src/lib/auth/address-link.ts`) and the sentence lives beside it, because the Google leg comes back as a redirect and can only carry the word. Do not replace any of this with Supabase's `linkIdentity`: it sends the browser to Supabase's own callback, which is the exact thing `src/lib/auth/google.ts` refuses to do, and it would put a hostname nobody recognises on the consent screen.
- **There was a second way to add an address, and it is gone on purpose.** Any typed address could be mailed a confirmation link carrying a token whose sha256 was all the table held, good for an hour, and `/auth/link` confirmed it while deliberately signing nobody in. It went with the magic link when Google became the only way in, because **an address you cannot sign in with is not worth confirming**: confirming one anyway promises somebody a way into their account that does not exist. Removed with it: `addAddress`, `startAddressLink`, `confirmAddressLink`, `sendLinkedSignIn`, `confirmUrlFor`, the token minting in `address-link.ts`, `link-mail.ts`, `email-mx.ts`, `sendTransactionalEmail`, `/auth/link`, `/auth/linked`, the `sent` and `no-mail` outcomes, the `address-taken` and `link-failed` error reasons, and the `address_link_requested` event. `readEmail` stays, because `isSendable` is what decides whether a **notification** may go to an address already on file, which is a different question from whether somebody typed one correctly. The `token_hash` and `token_expires_at` columns on `account_emails` stay for now and are always null; dropping them is a migration of its own. Do not reintroduce any of this to "let people add a work address": the answer is to sign in to that Google account and connect it.
- **A losing number is red, and red does not exist at L 0.74.** Every accent used to sit at one lightness, and for the aqua, the green and the amber that is still right. Red is the hue where the rule breaks: the most chromatic red sRGB has at 0.74 is `oklch(0.74 0.16 25)`, `#ff716b`, which is a salmon. Not a red held quietly, a different colour, and the one thing a `-2.5%` must not read as. So the two reds hold hue 25 (a degree off pure red's 29, and 20 clear of the amber at 45) and drop to where chroma is available, at two lightnesses because they do two jobs. `--loss` is text on the field, `oklch(0.66 0.22 25)` (`#fc4447`), the lightest point that is still unmistakably red: 6.1:1 on black, short of the salmon's 8.5:1 and well past the 4.5:1 AA asks of body text, because a number nobody reads as a loss is the worse failure. `--destructive` is a fill under white text and is measured the other way round, `oklch(0.58 0.232 25)` (`#e30a28`): white on it was **2.46:1** at the old lightness, a real failure that was shipping, and is 4.83:1 now. `HEX.loss` in `src/lib/brand/mark.ts` is the same colour for the mail clients and the share PNG, which cannot do oklch, and it moves whenever the token does. **Never raise either back to 0.74 for the sake of the row**: a palette rule that turns the loss colour into a salmon has stopped describing the product. The locked values are asserted in `tests/e2e/signed-out.spec.ts`, and the full account is in `docs/brand/ARENA_MARK.md`.
